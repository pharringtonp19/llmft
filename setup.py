from setuptools import setup, find_packages

setup(
    name='llmft',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # list libraries needed by your library to work
    ],
    author='Patrick Power',
    author_email='pharringtonp19@gmai.com',
    description='fine-tuning LLMs for Causal Inference Tasks',
    license='MIT',
    keywords='example project',
)